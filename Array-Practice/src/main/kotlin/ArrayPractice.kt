fun main() {
    //So we created array/massive
var numbers = arrayOf(165, 350, 151, 177, 644)
    // we can change element by using square brackets
    numbers[3] = 160
    // or can print element by using println and squares
    println(numbers[0])
    // or work with cycle
    numbers.forEach { payextra -> if (165 > 7)
        println("There number is so big!!!")
    }
}
